import styled from 'styled-components'

export const Container = styled.div`
  font-size: 0.8rem;
  color: #747577;
  svg {
    height: 1.1rem;
    width: 1.1rem;
  }

  display: flex;
  gap: 0.25rem;
`
